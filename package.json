{
  "name": "event-emitter",
  "version": "0.0.0",
  "description": "browserify module for emitting/registering to custom events in the browser",
  "main": "build/EventEmitter.js",
  "dependencies": {
    "browserify": "~4.2.3",
    "docme": "^0.2.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "make"
  },
  "author": "Jake Scott",
  "license": "MIT"
}
